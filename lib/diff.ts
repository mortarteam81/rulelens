export function simpleDiff(oldText:string,newText:string){const oldWords=oldText.split(/(\s+)/);const newWords=newText.split(/(\s+)/);const oldSet=new Set(oldWords.filter(w=>w.trim()));const newSet=new Set(newWords.filter(w=>w.trim()));const oldHtml=oldWords.map(w=>w.trim()&&!newSet.has(w)?`<del>${escapeHtml(w)}</del>`:escapeHtml(w)).join('');const newHtml=newWords.map(w=>w.trim()&&!oldSet.has(w)?`<ins>${escapeHtml(w)}</ins>`:escapeHtml(w)).join('');return {oldHtml,newHtml};}
function escapeHtml(s:string){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c] as string));}
